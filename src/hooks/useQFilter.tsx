// import { QFilterBuilder } from "@/lib";
// import { useRef } from "react";

// /**
//  * A custom hook that provides an instance of QFilterBuilder for the specified type T.
//  *
//  * @template T - The type of the data that the QFilterBuilder will work with.
//  * @returns {QFilterBuilder<T>} An instance of QFilterBuilder for type T.
//  */
// const useQFilter = <T,>(): QFilterBuilder<T> => {

//   return builder.current;
// };

// export default useQFilter;
